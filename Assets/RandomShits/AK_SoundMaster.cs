using UnityEngine;

public class AK_SoundMaster : MonoBehaviour
{
    public static float AK_SoundMaster_All_Volume = 1;

    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
